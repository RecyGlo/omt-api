/* eslint-disable no-underscore-dangle */
/* eslint-disable no-plusplus */

const GREET = async (req, res) => res.status(200).send('Welcome to Oh My Trash API');

module.exports = {
  GREET,
};
